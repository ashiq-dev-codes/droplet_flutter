import 'package:flutter/material.dart';

/// Design system colors extracted from the Figma fitness tracker design.
class AppColors {
  // Core surfaces
  static const Color background = Color(0xFFF4F2EC); // cream app background
  static const Color surface = Color(0xFFEBE8E0); // cards / surfaces
  static const Color surfaceBorder = Color(0xFFE3E0D7); // card borders

  // Text
  static const Color ink = Color(0xFF0E0F0C); // primary text
  static const Color inkSoft = Color(0xFF8A8A82); // secondary text

  // Accents
  static const Color accentOrange = Color(0xFFFF5A1F);
  static const Color accentLime = Color(0xFFD7FE47);
  static const Color accentBlue = Color(0xFF2B7FFF);
  static const Color accentYellow = Color(0xFFF0B100);
  static const Color accentPurple = Color(0xFFAD46FF);
  static const Color accentRed = Color(0xFFF54900);

  // Neutrals
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Dark nav surface
  static const Color nav = Color(0xFF171812); // dark pill nav bar
}
